import { StyleSheet } from 'react-native';

export const baseStyles = StyleSheet.create({
  profilePic: {
    width: 100,
    height: 100,
    borderRadius: 50,
    borderWidth: 3,
    borderColor: '#2196F3',
  },
  // Add all your theme-independent styles here
});